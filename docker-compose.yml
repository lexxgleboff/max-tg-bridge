services:
  bridge:
    build: .
    restart: unless-stopped
    env_file: .env
    environment:
      TZ: Europe/Moscow
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"
